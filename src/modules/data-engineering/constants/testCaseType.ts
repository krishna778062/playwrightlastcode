/**
 * Test Case Type Tags
 *
 * These tags categorize test cases by the type of metric or visualization being tested.
 * Used to filter and organize test execution by test case type.
 */
export enum TestCaseType {
  HERO_METRIC = '@hero-metric',
  TABULAR_METRIC = '@tabular-metric',
  PIE_CHART = '@pie-chart',
  BAR_CHART = '@bar-chart',
  LINE_CHART = '@line-chart',
  CSV_VALIDATION = '@csv-validation',
}
