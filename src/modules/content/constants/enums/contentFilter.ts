/**
 * Content filter options for managing content visibility
 * Used in manage site content and manage content pages
 */
export enum ContentFilter {
  MANAGING = 'managing',
  OWNED = 'owned',
}

/**
 * Human-readable labels for content filters
 */
export enum ContentFilterLabel {
  MANAGING = 'Managed by me',
  OWNED = 'Authored by me',
}
