export enum SITE_TYPES {
  PUBLIC = 'public',
  PRIVATE = 'private',
  UNLISTED = 'unlisted',
  SITE = 'site',
  CATEGORY = 'Uncategorized',
}

export enum SITE_ACCESS_TYPES {
  PUBLIC = 'Public',
  PRIVATE = 'Private',
  UNLISTED = 'Unlisted',
}
