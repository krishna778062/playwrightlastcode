name: UAT Release Regression Tests

on:
  schedule:
    # Run every alternate day (Mon, Wed, Fri) at 1:30 AM UTC (7:00 AM IST)
    - cron: '30 1 * * 1,3,5'
  workflow_dispatch:

jobs:
  regression:
    uses: ./.github/workflows/uat-regression-template.yml
    with:
      environment: uat
    secrets: inherit
