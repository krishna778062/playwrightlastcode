name: Prod EU Healthcheck

on:
  schedule:
    # Run daily at 6 AM UTC
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  healthcheck:
    uses: ./.github/workflows/prod-healthcheck-template.yml
    with:
      environment: prodEU
      git_ref: main
    secrets: inherit
