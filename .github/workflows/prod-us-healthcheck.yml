name: Prod US Healthcheck

on:
  schedule:
    # Run daily at 2 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  healthcheck:
    uses: ./.github/workflows/prod-healthcheck-template.yml
    with:
      environment: prodUS
      git_ref: main
    secrets: inherit
